# Backend - Sports Performance Pro

## Getting Started

1. `npm install`
2. Set environment variable `JWT_SECRET`
3. `npm start` (runs on port 4000)

Endpoints:
- POST `/api/login`
- GET `/api/profile`
